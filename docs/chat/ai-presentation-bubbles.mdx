---
title: "AI presentation bubbles"
description: "Markdown, research answer and the AiBubble block that frames every agent reply."
icon: "sparkles"
---

Spec folder: [`specs/003-ai-presentation-bubbles`](https://github.com/exegia/corpora-ui/tree/dev/specs/003-ai-presentation-bubbles). Sketch masters: `Component / Markdown / Preview|Markup`, `Component / Research Answer`, `Block / AI Bubble / Markdown|Research Answer|Chart|Streaming` in the `exegia-ui` document (cloud `01945ba6-1dcb-4f55-bc46-c0cf4d09aa7c`). Every component carries its master name in a `@sketch` JSDoc tag.

## Problem

Agent replies need a consistent frame (sparkles tile · Exegia · Agent badge · time) around several content types: rendered markdown with a source toggle, a research answer with citation meta and actions, a chart card, and a streaming answer.

## Solution

- Atoms: `SegmentedToggle` (radiogroup, arrow keys, controlled or uncontrolled), `IconButton`, `AgentBadge`.
- `Markdown`: Preview | Markup toggle over react-markdown, copy and expand callbacks; the pane lives in `markdownViewAtom(id)`.
- `ResearchAnswer`: kicker, content, Source / Date / Author(s) meta, Copy citation · Share · Add to list · thumbs.
- `AiBubble`: agent header plus a discriminated `content` union — `markdown`, `research`, `chart`, `streaming`.

## UI reference

<Frame caption="AI presentation bubbles — Light (Sketch frame)">
  <img src="/images/ai-presentation-light.png" alt="AI presentation bubbles light showcase" />
</Frame>
<Frame caption="AI presentation bubbles — Dark (Sketch frame)">
  <img src="/images/ai-presentation-dark.png" alt="AI presentation bubbles dark showcase" />
</Frame>

## Usage

```tsx
import { AiBubble } from "@corpora/ui"

<AiBubble time="2 min ago" content={{ kind: "markdown", source: answer }} />
<AiBubble content={{ kind: "research", corpus: "Iliad", content: "…", source: "Iliad · Homer corpus" }} />
```

## Acceptance criteria

Copied from `specs/003-ai-presentation-bubbles/spec.md`. Each holds in light and dark.

#### Atoms
- [ ] AC-A1 SegmentedToggle 117×24: `surface-subtle` track radius 6, active segment card surface with shadow, labels 11px; keyboard arrows move selection; `value`/`onValueChange` controlled and `defaultValue` uncontrolled; `sound` prop emits toggle cue.
- [ ] AC-A2 IconButton 24px, icon 16px in `--icon`, hover `surface-subtle`, `aria-label` required.
- [ ] AC-A3 AgentBadge 48×20 radius 4, `accent-subtle` fill, `accent-text` label "Agent".

#### Markdown
- [ ] AC-M1 360 wide card radius 12; header row: toggle left, expand + copy right; 1px divider.
- [ ] AC-M2 Preview renders h1 15/600, paragraph 13, bullet list, inline code / fenced code in `surface-code` with `--link` coloured command text.
- [ ] AC-M3 Markup shows raw source in a mono block (`surface-code`, 11px) with the same toggle state.
- [ ] AC-M4 `onCopy(source)` and `onExpand()` callbacks; toggle state via keyed atom `markdownViewAtom(id)` so an app can flip Preview/Markup by id.

#### ResearchAnswer
- [ ] AC-R1 380 wide card radius 12; kicker row with 28px IconTile (book-open), "Research answer" 13/600, sub 11 secondary, corpus Pill right.
- [ ] AC-R2 Content 13px; divider; three meta columns each icon 12px + label 11 secondary + value 12 primary (Source, Date, Author(s)).
- [ ] AC-R3 Actions row: Copy citation · Share · Add to list (ghost buttons with icons) left, thumbs-up / thumbs-down IconButtons right.
- [ ] AC-R4 Callbacks: `onCopyCitation`, `onShare`, `onAddToList`, `onFeedback("up"|"down")`. Behaviour of "Add to list" is not designed (TODO).

#### AiBubble
- [ ] AC-B1 Header: 32px sparkles IconTile, name 13/600, AgentBadge, time 11 muted on second line; content indented 40px.
- [ ] AC-B2 `content="markdown"` renders Markdown; `"research"` renders ResearchAnswer; `"chart"` renders Chart; `"streaming"` renders StreamingText — each accepting that component's props.
- [ ] AC-B3 Light/dark by tokens; registry entries + demos; tests for SegmentedToggle keyboard/controlled and Markdown toggle; `make check`.

## Open questions

- Expand: frame shows a maximize icon only. Callback only.
- Copy: copies markdown source in both views (assumption).

## Related issues

- [#139](https://github.com/exegia/corpora-ui/issues/139)
- [#144](https://github.com/exegia/corpora-ui/issues/144)
- [#148](https://github.com/exegia/corpora-ui/issues/148)
- [#152](https://github.com/exegia/corpora-ui/issues/152)
