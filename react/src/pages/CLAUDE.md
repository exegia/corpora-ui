---
name: Custom Fumapress pages
description: React routes for custom designs only.
---

# Custom pages

Documentation and category pages belong in `content/` as `.md` or `.mdx`.
Only add a route here when a custom design cannot use the standard docs layout.
Keep route files thin. Validate routes with `bun run build:docs`.
