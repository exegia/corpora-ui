---
title: "Code block"
description: "Code card with filename, Code | Diff toggle and Copy; gutter line numbers and keyword / string tinting from the --code-* tokens. No highlighter dependency."
---

import { Preview } from "@/components/composed/code-block.story"

import Demo from "@/registry/demos/code-block-demo"

## Preview

Use the controls to explore the component's props.

<Preview />

## Props

| Prop                                | Type                                   | Default | Description                 |
| ----------------------------------- | -------------------------------------- | ------- | --------------------------- |
| `code` (required)                   | `string`                               | —       | Source; split on newlines.  |
| `filename`                          | `ReactNode`                            | —       | Header label.               |
| `diff`                              | `{ type?: "add" \| "remove", text }[]` | —       | Enables the Diff view.      |
| `view / defaultView / onViewChange` | `"code" \| "diff"`                     | —       | Controlled or uncontrolled. |
| `onCopy`                            | `(code) => void`                       | —       | Copy button.                |
| `keywords`                          | `string[]`                             | —       | Extra keywords to tint.     |

## Example

<Demo />

```tsx
import { CodeBlock } from "@exegia/corpora-ui"

;<CodeBlock filename="churn.ts" code={source} onCopy={copy} />
```
