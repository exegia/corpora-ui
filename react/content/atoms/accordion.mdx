---
title: "Accordion"
description: "Expandable sections with keyboard navigation."
---

import { Preview } from "@/components/stories/accordion.story"

<Preview />

## Usage

Import the stylesheet once at the application root: `@exegia/corpora-ui/index.css`.

```tsx
import { Accordion, AccordionItem, AccordionTrigger, AccordionPanel } from "@exegia/corpora-ui/ui/accordion"

function ComponentPreview({ multiple }: { multiple: boolean }) {

  return (
    <div className="relative mx-auto w-full max-w-lg p-6">
      <Accordion multiple={multiple}>
      <AccordionItem value="sources"><AccordionTrigger>Which sources are included?</AccordionTrigger><AccordionPanel>Published editions and their annotations.</AccordionPanel></AccordionItem>
      <AccordionItem value="export"><AccordionTrigger>Can I export a passage?</AccordionTrigger><AccordionPanel>Choose an export format from the passage menu.</AccordionPanel></AccordionItem>
    </Accordion>
    </div>
  )
}

<ComponentPreview multiple={false} />
```

## API notes

`multiple` permits more than one expanded item. Use `defaultValue` for initial values or `value` with `onValueChange` for controlled state. Every item needs a stable `value`.
