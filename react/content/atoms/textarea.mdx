---
title: Textarea
description: Multi-line text input.
icon: Pencil
---

import { Preview } from "@/components/stories/textarea.story"

Use the story controls to explore the `Textarea` atom's properties.

<Preview />

## Props

### Basic functionality

- **placeholder**: Hint text shown when the field is empty
- **disabled**: Disables the textarea
- **value**: Current value (controlled component)

## Usage

This UI module is also available from `@exegia/corpora-ui/ui/textarea`.

```tsx
import { Textarea } from "@exegia/corpora-ui"

<Textarea placeholder="Add an annotation…" />
<Textarea disabled placeholder="Disabled textarea" />
```
