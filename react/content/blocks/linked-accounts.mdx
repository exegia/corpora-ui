---
title: "Linked accounts"
description: "Settings panel for the sign-in identities on an account: lists the connected ones, offers connect buttons for the rest, and guards the last remaining method from being disconnected."
---

import { Preview } from "@/components/stories/linked-accounts-block.story"
import Demo from "@/registry/demos/linked-accounts-demo"

## Preview and props

Expand **Props** below the centered preview to change the component’s properties.

<Preview />

## Examples

Connect and disconnect demo identities, and explore loading and error states.

<Demo />

## Usage

```tsx
import { LinkedAccountsBlock } from "@exegia/corpora-ui"

<LinkedAccountsBlock identities={identities} onLink={link} onUnlink={unlink} />
```

## API notes

| Prop              | Type                                                        | Default                                    | Description                                                                                     |
| ----------------- | ----------------------------------------------------------- | ------------------------------------------ | ----------------------------------------------------------------------------------------------- |
| identities        | <code>ILinkedIdentity[]</code>                               | <code>[]</code>                            | Identities attached to the account (&#123; id, provider, email? &#125;).                        |
| providers         | <code>TSocialProvider[]</code>                               | <code>["google", "apple", "github"]</code> | Connect candidates. Already-connected providers are filtered out.                               |
| loading           | <code>boolean</code>                                        | <code>false</code>                         | Shows the loading row instead of the list.                                                      |
| onLink / onUnlink | <code>(provider &#124; id) =&gt; Promise&lt;void&gt</code> | —                                          | Reject (or throw) to show the inline error. Unlink never fires for the last remaining identity. |
